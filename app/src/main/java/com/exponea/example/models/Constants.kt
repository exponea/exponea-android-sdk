package com.exponea.example.models

object Constants {
    object ScreenNames {
        var mainScreen = "Main"
        var purchaseScreen = "Purchase"
        var settingsScreen = "Settings"
        var anonymizeScreen = "Anonymize"
        var inAppContentBlocksScreen = "InAppCB"
    }

    const val CUSTOMER_ID_REGISTERED = "registered"
}
